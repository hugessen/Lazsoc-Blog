import { GetLongDatePipe } from './get-long-date.pipe';

describe('GetLongDatePipe', () => {
  it('create an instance', () => {
    const pipe = new GetLongDatePipe();
    expect(pipe).toBeTruthy();
  });
});
